node_modules
.pnpm-store
.next
out
build
dist
.turbo
*.tsbuildinfo
next-env.d.ts

# No environment files are tracked, templates included: .env.example carries
# live sandbox credentials and is provisioned out of band.
.env
.env.*

# Local PGlite data directory.
.data

# Landing page explorations. Local only.
sample

# Working notes on product and design decisions. Local only.
memory

# Agent sessions and their worktrees — each one is a full checkout copy.
.claude

# Scratch: generated imagery and one-off scripts kept out of the history.
output
tmp

.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
coverage
